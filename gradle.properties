org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# Rokid CXR-M SDK — replace with your actual secret (no hyphens) from the Rokid developer portal.
# Never commit the real value; override it in local.properties which is .gitignored.
ROKID_CLIENT_SECRET=YOUR_SECRET_HERE
